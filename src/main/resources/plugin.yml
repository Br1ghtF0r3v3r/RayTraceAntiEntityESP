main: ${main}
api-version: ${apiVersion}

name: ${name}
version: ${version}
author: ${author}

softdepend: ["packetevents"]

permissions:
  rtaee.command:
    default: op

commands:
  rtaee:
    permission: rtaee.command
